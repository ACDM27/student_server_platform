import {
  createContext
} from "./chunk-DPXI3MID.js";
import {
  ForwardRef$5j,
  Link
} from "./chunk-HTHSVWFP.js";
import {
  useIntl
} from "./chunk-T6ZAQSJD.js";
import {
  NavLink,
  useNavigate
} from "./chunk-WRC7634M.js";
import {
  require_jsx_runtime
} from "./chunk-OWKUITKV.js";
import {
  require_react
} from "./chunk-I3AIFIJ5.js";
import {
  __toESM
} from "./chunk-PLDDJCW6.js";

// node_modules/.pnpm/@strapi+admin@5.18.0_@babel_49941d4988586d24fe6eaaa483d9aeaa/node_modules/@strapi/admin/dist/admin/admin/src/features/BackButton.mjs
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
var React = __toESM(require_react(), 1);
var [Provider, useHistory] = createContext("History", {
  history: [],
  currentLocationIndex: 0,
  currentLocation: "",
  canGoBack: false,
  pushState: () => {
    throw new Error("You must use the `HistoryProvider` to access the `pushState` function.");
  },
  goBack: () => {
    throw new Error("You must use the `HistoryProvider` to access the `goBack` function.");
  }
});
var BackButton = React.forwardRef(({ disabled, fallback = "" }, ref) => {
  const { formatMessage } = useIntl();
  const navigate = useNavigate();
  const canGoBack = useHistory("BackButton", (state) => state.canGoBack);
  const goBack = useHistory("BackButton", (state) => state.goBack);
  const history = useHistory("BackButton", (state) => state.history);
  const currentLocationIndex = useHistory("BackButton", (state) => state.currentLocationIndex);
  const hasFallback = fallback !== "";
  const shouldBeDisabled = disabled || !canGoBack && !hasFallback;
  const handleClick = (e) => {
    e.preventDefault();
    if (canGoBack) {
      goBack();
    } else if (hasFallback) {
      navigate(fallback);
    }
  };
  const historyTo = canGoBack ? history.at(currentLocationIndex - 2) : void 0;
  const toWithFallback = historyTo ?? fallback;
  return (0, import_jsx_runtime.jsx)(Link, {
    ref,
    tag: NavLink,
    to: toWithFallback,
    onClick: handleClick,
    disabled: shouldBeDisabled,
    "aria-disabled": shouldBeDisabled,
    startIcon: (0, import_jsx_runtime.jsx)(ForwardRef$5j, {}),
    children: formatMessage({
      id: "global.back",
      defaultMessage: "Back"
    })
  });
});

export {
  BackButton
};
//# sourceMappingURL=chunk-UDZ4CA2T.js.map
